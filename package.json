{
  "name": "freefocusapp",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "dev": "pnpm --filter @focus/web dev",
    "build": "pnpm --filter @focus/web build",
    "preview": "pnpm --filter @focus/web preview",
    "typecheck": "pnpm --filter @focus/web exec tsc -b",
    "test": "pnpm --filter @focus/web test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/freeappstore-online/focus.git"
  }
}
